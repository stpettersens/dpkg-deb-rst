install:
    - choco install rust -version 1.10.0 
    #- choco install ruby
    - ps: refreshenv
    #- gem install os
    #- rake --version
    #- rustc --version
    - tree "C:\Program Files\Rust stable GNU 1.10\bin"

test_script:
    - rake
    - rake test
