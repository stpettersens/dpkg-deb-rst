install:
    - choco install rust -version 1.10.0 
    #- choco install ruby
    - ps: refreshenv
    #- gem install os
    #- rake --version
    - where rustc
    #- cargo --version

test_script:
    - rake
    - rake test
