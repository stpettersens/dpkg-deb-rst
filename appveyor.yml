install:
    - choco install rust ruby # Install Rust & Ruby.
    - ps: refreshenv
    - gem install os # Install os gem.
    - ruby --version
    - rustc --version
    - cargo --version

test_script:
    - rake
    - rake test
